{
  "name": "cognito-migration-lambda",
  "version": "1.0.0",
  "description": "Lambda function for Cognito cross-account user migration",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "lint": "eslint .",
    "package": "zip -r migration-function.zip .",
    "deploy": "aws lambda update-function-code --function-name cognito-migration --zip-file fileb://migration-function.zip"
  },
  "dependencies": {
    "aws-sdk": "^2.1691.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "eslint": "^8.57.0",
    "@aws-sdk/client-cognito-identity-provider": "^3.658.1"
  },
  "keywords": [
    "aws",
    "lambda",
    "cognito",
    "migration",
    "cross-account"
  ],
  "author": "Your Name",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}